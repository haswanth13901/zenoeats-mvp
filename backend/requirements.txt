fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.31
# Menu image uploads: checked, re-encoded to WebP and stripped of metadata
# before they are stored. Built with WebP support in the published wheels.
Pillow==12.3.0
PyJWT[crypto]==2.14.0
stripe==15.6.1
celery==5.4.0
redis==8.1.0
cryptography==50.0.1
svix==1.44.0
httpx==0.28.1
pytest==9.0.3
# Password hashing for platform-admin and (later) restaurant credentials.
argon2-cffi==23.1.0
# Error tracking. Inert unless SENTRY_DSN is set.
sentry-sdk==2.69.1
# IANA timezones for zoneinfo: restaurant-local report days. Slim images and
# Windows have no system zone database.
tzdata==2026.3
