# Without this the whole node_modules tree and every previous build are
# uploaded to the daemon as build context before the first instruction runs.
# That made `docker compose build web` take minutes and repeatedly time out.
node_modules
.next
.clerk
.env
.env.*
npm-debug.log*
.git
.gitignore
Dockerfile
.dockerignore
README.md
